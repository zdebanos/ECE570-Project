[project]
name = "project-impl"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "solar-milp-model",
]

[tool.uv.workspace]
members = ["packages/*"]

[tool.uv.sources]
solar-milp-model = { workspace = true }
