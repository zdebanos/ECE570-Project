[project]
name = "solar-mpc"
version = "0.1.0"
description = "A Solar MPC class interface"
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.4.2",
]

[tool.hatch.build.targets.wheel]
packages = ["src/solar_mpc"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
